---
phase: 01-foundation-and-authentication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/tsconfig.json
  - frontend/app/layout.tsx
  - frontend/app/globals.css
  - frontend/components.json
  - backend/package.json
  - backend/tsconfig.json
  - backend/src/app.ts
  - backend/src/server.ts
  - backend/src/config/env.ts
  - backend/src/config/db.ts
  - backend/.env.example
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
user_setup: []
must_haves:
  truths:
    - Bun-managed Next.js app exists in frontend with App Router, Tailwind CSS, and shadcn/ui configured for the new-york preset
    - TypeScript Express app exists in backend with build, dev, and test scripts plus environment and MongoDB bootstrap
    - Frontend and backend share explicit environment contracts for API origin, MongoDB URI, JWT secret, and client origin
  artifacts:
    - frontend/package.json
    - frontend/components.json
    - frontend/app/layout.tsx
    - backend/package.json
    - backend/src/app.ts
    - backend/src/server.ts
    - backend/src/config/env.ts
    - backend/src/config/db.ts
    - backend/.env.example
  key_links:
    - frontend package scripts and config files support Bun-based execution for the app shell work in later plans
    - backend bootstrap loads validated environment values before starting Express or MongoDB connections
---

<objective>
Create the shared project foundation for Phase 1 by scaffolding the frontend and backend applications, standardizing TypeScript/tooling, and defining the environment contracts that later auth and UI plans will rely on.

Purpose: Every later Phase 1 task depends on a real app structure, stable scripts, and explicit config contracts rather than ad hoc setup.
Output: Working frontend/backend scaffolds, env examples, and baseline app/bootstrap files that build cleanly.
</objective>

<execution_context>
@/Users/satish/Desktop/AI-Powered Learning Assistant/.codex/get-shit-done/workflows/execute-plan.md
@/Users/satish/Desktop/AI-Powered Learning Assistant/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-authentication/01-CONTEXT.md
@.planning/phases/01-foundation-and-authentication/01-RESEARCH.md
@.planning/phases/01-foundation-and-authentication/01-UI-SPEC.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold frontend with Bun, Next.js App Router, Tailwind, and shadcn/ui</name>
  <files>frontend/package.json, frontend/tsconfig.json, frontend/app/layout.tsx, frontend/app/globals.css, frontend/components.json</files>
  <read_first>.planning/PROJECT.md, .planning/phases/01-foundation-and-authentication/01-CONTEXT.md, .planning/phases/01-foundation-and-authentication/01-RESEARCH.md, .planning/phases/01-foundation-and-authentication/01-UI-SPEC.md, AGENTS.md</read_first>
  <action>Inside `frontend/`, create the app with `bun create next-app@latest .` using TypeScript and App Router. Install and configure Tailwind CSS plus shadcn/ui using the `new-york` preset from the UI spec. Ensure `frontend/package.json` exposes at least `dev`, `build`, `lint`, and `start` scripts. Replace the default layout/global styles so the app uses `Plus Jakarta Sans` in `frontend/app/layout.tsx`, establishes the spacing/color tokens from `01-UI-SPEC.md` in `frontend/app/globals.css`, and leaves the route groups `(marketing)`, `(auth)`, and `(app)` ready for later plans instead of keeping the stock starter page.</action>
  <verify>test -f frontend/components.json && rg -n 'Plus Jakarta Sans|0F766E|F8F6F1|new-york' frontend/app/layout.tsx frontend/app/globals.css frontend/components.json && cd frontend && bun run build</verify>
  <acceptance_criteria>
    - `frontend/components.json` exists and contains `"style": "new-york"`
    - `frontend/app/layout.tsx` contains `Plus Jakarta Sans`
    - `frontend/app/globals.css` contains `#F8F6F1` and `#0F766E`
    - `frontend/package.json` contains `"build"` and `"lint"` scripts
  </acceptance_criteria>
  <done>Frontend scaffold exists, honors the UI contract, and `cd frontend && bun run build` exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold backend TypeScript Express app with validated config and MongoDB bootstrap</name>
  <files>backend/package.json, backend/tsconfig.json, backend/src/app.ts, backend/src/server.ts, backend/src/config/env.ts, backend/src/config/db.ts, backend/.env.example</files>
  <read_first>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/phases/01-foundation-and-authentication/01-CONTEXT.md, .planning/phases/01-foundation-and-authentication/01-RESEARCH.md, AGENTS.md</read_first>
  <action>Inside `backend/`, initialize a TypeScript Node/Express service. Add dependencies for `express`, `mongoose`, `zod`, `dotenv`, `cookie-parser`, `cors`, `helmet`, and development tooling for TypeScript/Vitest execution. Create `backend/src/config/env.ts` that validates `PORT`, `MONGODB_URI`, `JWT_SECRET`, `CLIENT_ORIGIN`, and `NODE_ENV`. Create `backend/src/config/db.ts` for Mongoose connection bootstrap. Create `backend/src/app.ts` to assemble Express with `helmet`, `cors({ origin: CLIENT_ORIGIN, credentials: true })`, `cookie-parser`, JSON parsing, and a base health route at `/api/health`. Create `backend/src/server.ts` to load env, connect MongoDB, and start the server. Write `backend/.env.example` with those exact keys.</action>
  <verify>rg -n 'PORT=|MONGODB_URI=|JWT_SECRET=|CLIENT_ORIGIN=' backend/.env.example && rg -n 'cors\\(|credentials: true|cookieParser|/api/health' backend/src/app.ts && cd backend && npm run build</verify>
  <acceptance_criteria>
    - `backend/.env.example` contains `PORT=`, `MONGODB_URI=`, `JWT_SECRET=`, and `CLIENT_ORIGIN=`
    - `backend/src/app.ts` contains `cookieParser`, `credentials: true`, and `/api/health`
    - `backend/src/server.ts` contains `connect` or `connectDB` bootstrap before `listen`
    - `backend/package.json` contains `dev`, `build`, and `test` scripts
  </acceptance_criteria>
  <done>Backend scaffold boots from validated env/config and `cd backend && npm run build` exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: Establish cross-app conventions and baseline verification tooling</name>
  <files>frontend/package.json, backend/package.json, backend/vitest.config.ts, backend/src/test/setup.ts</files>
  <read_first>frontend/package.json, backend/package.json, .planning/phases/01-foundation-and-authentication/01-VALIDATION.md, AGENTS.md</read_first>
  <action>Add the initial test/build conventions needed by the validation strategy. Create `backend/vitest.config.ts` and `backend/src/test/setup.ts` so later auth tests have a stable place to live. Ensure `backend/package.json` has `test` and `test:watch` scripts using Vitest, and confirm the frontend package scripts remain Bun-friendly. Do not implement auth tests yet; only create the harness and scripts that later plans will use. Keep the root repo on the current single-repo structure without creating nested git repos.</action>
  <verify>test -f backend/vitest.config.ts && test -f backend/src/test/setup.ts && rg -n 'vitest|test:watch|test' backend/package.json</verify>
  <acceptance_criteria>
    - `backend/vitest.config.ts` exists
    - `backend/src/test/setup.ts` exists
    - `backend/package.json` contains `vitest` and `test:watch`
    - no `.git` directory exists inside `frontend/` or `backend/`
  </acceptance_criteria>
  <done>Baseline verification tooling exists and later auth plans can add tests without reworking the backend scaffold.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && bun run build`
- [ ] `cd backend && npm run build`
- [ ] `test -f frontend/components.json && test -f backend/vitest.config.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No nested git repos created inside `frontend/` or `backend/`
- Frontend and backend scaffolds are ready for auth and app-shell implementation in later plans
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-authentication/01-01-SUMMARY.md`
</output>
