---
phase: 01-foundation-and-authentication
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - frontend/app/(marketing)/page.tsx
  - frontend/app/(auth)/page.tsx
  - frontend/app/(app)/layout.tsx
  - frontend/app/(app)/page.tsx
  - frontend/components/app-shell/app-sidebar.tsx
  - frontend/components/app-shell/app-header.tsx
  - frontend/components/auth/auth-form.tsx
  - frontend/lib/api.ts
  - frontend/lib/auth.ts
  - frontend/middleware.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
user_setup: []
must_haves:
  truths:
    - Public users land on a marketing page with the primary CTA "Start Studying"
    - A single auth page supports both sign up and login against the real backend auth API
    - Successful auth routes users into a left-sidebar app shell whose first screen is the dashboard/home workspace
    - Logout clears the current-browser session and protected app routes redirect unauthenticated users away from the app shell
  artifacts:
    - frontend/app/(marketing)/page.tsx
    - frontend/app/(auth)/page.tsx
    - frontend/app/(app)/layout.tsx
    - frontend/app/(app)/page.tsx
    - frontend/components/auth/auth-form.tsx
    - frontend/components/app-shell/app-sidebar.tsx
    - frontend/components/app-shell/app-header.tsx
    - frontend/lib/api.ts
    - frontend/lib/auth.ts
  key_links:
    - frontend auth requests call backend /api/auth endpoints with `credentials: "include"`
    - protected app routes use frontend auth helpers that depend on the cookie-based session created by Plan 02
    - app shell content and copy match the locked Phase 1 UI contract
---

<objective>
Build the public landing page, single auth flow, and authenticated app shell, then connect them to the real backend cookie-auth API.

Purpose: This plan turns the backend auth foundation into an actual user-facing Phase 1 experience that satisfies the roadmap and UI contract.
Output: landing page, auth page, protected app shell, auth client utilities, route protection, and logout behavior.
</objective>

<execution_context>
@/Users/satish/Desktop/AI-Powered Learning Assistant/.codex/get-shit-done/workflows/execute-plan.md
@/Users/satish/Desktop/AI-Powered Learning Assistant/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-authentication/01-CONTEXT.md
@.planning/phases/01-foundation-and-authentication/01-UI-SPEC.md
@frontend/app/layout.tsx
@frontend/app/globals.css
@backend/src/modules/auth/auth.routes.ts
@backend/src/middleware/require-auth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the public landing page and single auth screen from the UI contract</name>
  <files>frontend/app/(marketing)/page.tsx, frontend/app/(auth)/page.tsx, frontend/components/auth/auth-form.tsx</files>
  <read_first>frontend/app/layout.tsx, frontend/app/globals.css, .planning/phases/01-foundation-and-authentication/01-CONTEXT.md, .planning/phases/01-foundation-and-authentication/01-UI-SPEC.md, backend/src/modules/auth/auth.routes.ts</read_first>
  <action>Create `frontend/app/(marketing)/page.tsx` as the public landing screen with the primary CTA text `Start Studying`, academic/playful tone, and a clear path into `/auth`. Create a single auth page at `frontend/app/(auth)/page.tsx` that renders one shared `AuthForm` component with a sign-up/login mode switch rather than separate pages. The form fields must include `name`, `email`, and `password` for sign up, and `email` plus `password` for login. Keep copy aligned to the UI spec, including the error message pattern `We couldn't sign you in. Check your email and password, then try again.`</action>
  <verify>rg -n 'Start Studying|We couldn.t sign you in|sign up|log in' 'frontend/app/(marketing)/page.tsx' 'frontend/app/(auth)/page.tsx' frontend/components/auth/auth-form.tsx && cd frontend && bun run build</verify>
  <acceptance_criteria>
    - `frontend/app/(marketing)/page.tsx` contains `Start Studying`
    - `frontend/app/(auth)/page.tsx` or `frontend/components/auth/auth-form.tsx` contains both sign-up and login mode handling
    - auth form files contain `name`, `email`, and `password`
    - frontend build exits 0
  </acceptance_criteria>
  <done>Public and auth entry surfaces exist and match the locked Phase 1 UX/copy decisions.</done>
</task>

<task type="auto">
  <name>Task 2: Implement auth client integration, cookie-aware API helpers, and route protection</name>
  <files>frontend/lib/api.ts, frontend/lib/auth.ts, frontend/middleware.ts, frontend/app/(auth)/page.tsx, frontend/components/auth/auth-form.tsx</files>
  <read_first>backend/src/modules/auth/auth.routes.ts, backend/src/middleware/require-auth.ts, frontend/components/auth/auth-form.tsx, .planning/phases/01-foundation-and-authentication/01-CONTEXT.md, .planning/phases/01-foundation-and-authentication/01-RESEARCH.md</read_first>
  <action>Create `frontend/lib/api.ts` with a shared fetch helper that uses `credentials: "include"` and reads `NEXT_PUBLIC_API_URL`. Create `frontend/lib/auth.ts` with helpers for `signup`, `login`, `logout`, and `getCurrentUser`, targeting `/api/auth/signup`, `/api/auth/login`, `/api/auth/logout`, and `/api/auth/me`. Add `frontend/middleware.ts` or an equivalent protected-route guard so users who are not authenticated cannot stay on the `(app)` routes. On successful signup/login, route the user into the app shell. On logout, call `/api/auth/logout`, clear client auth state, and redirect back to the public side.</action>
  <verify>rg -n 'credentials: .include.|/api/auth/signup|/api/auth/login|/api/auth/logout|/api/auth/me' frontend/lib/api.ts frontend/lib/auth.ts && rg -n '(app)|/auth|/login|logout' frontend/middleware.ts 'frontend/app/(auth)/page.tsx' frontend/components/auth/auth-form.tsx</verify>
  <acceptance_criteria>
    - `frontend/lib/api.ts` contains `credentials: "include"`
    - `frontend/lib/auth.ts` contains all four auth endpoint paths
    - protected-route logic exists for the `(app)` surface
    - logout flow routes users away from the app shell
  </acceptance_criteria>
  <done>Frontend auth flows use the real backend cookie-auth API and enforce protected-route behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Build the authenticated dashboard shell with left sidebar and minimal live navigation</name>
  <files>frontend/app/(app)/layout.tsx, frontend/app/(app)/page.tsx, frontend/components/app-shell/app-sidebar.tsx, frontend/components/app-shell/app-header.tsx</files>
  <read_first>frontend/app/layout.tsx, frontend/app/globals.css, frontend/lib/auth.ts, .planning/phases/01-foundation-and-authentication/01-CONTEXT.md, .planning/phases/01-foundation-and-authentication/01-UI-SPEC.md</read_first>
  <action>Create the authenticated shell in `frontend/app/(app)/layout.tsx` using a left sidebar and header. The header must contain only app name/logo and a user menu with logout. The sidebar must show only live Phase 1 sections; do not include disabled future nav items. Build `frontend/app/(app)/page.tsx` as the dashboard/home workspace and include the empty-state heading `No documents yet` with body copy `Upload your first PDF to start studying.` as the main workspace message. Keep the visual system aligned with the approved UI contract colors, spacing, and typography.</action>
  <verify>rg -n 'No documents yet|Upload your first PDF to start studying.|logout|sidebar|Start Studying' 'frontend/app/(app)/page.tsx' frontend/components/app-shell/app-sidebar.tsx frontend/components/app-shell/app-header.tsx && cd frontend && bun run build</verify>
  <acceptance_criteria>
    - `frontend/app/(app)/page.tsx` contains `No documents yet`
    - `frontend/app/(app)/page.tsx` contains `Upload your first PDF to start studying.`
    - `frontend/components/app-shell/app-header.tsx` contains logout handling
    - sidebar files do not contain `coming soon` or disabled future sections
  </acceptance_criteria>
  <done>Authenticated users land in a real dashboard/home shell that matches the locked navigation and copy decisions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && bun run build`
- [ ] `rg -n 'credentials: "include"|Start Studying|No documents yet|Upload your first PDF to start studying.' frontend`
- [ ] auth flows redirect into `(app)` and logout returns to a public route
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Public landing, auth entry, and protected shell exist as a coherent Phase 1 experience
- Frontend and backend communicate through the real cookie-based auth contract
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-authentication/01-03-SUMMARY.md`
</output>
