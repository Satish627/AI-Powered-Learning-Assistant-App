---
phase: 01-foundation-and-authentication
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - backend/src/modules/auth/auth.model.ts
  - backend/src/modules/auth/auth.schemas.ts
  - backend/src/modules/auth/auth.service.ts
  - backend/src/modules/auth/auth.controller.ts
  - backend/src/modules/auth/auth.routes.ts
  - backend/src/middleware/require-auth.ts
  - backend/src/lib/jwt.ts
  - backend/src/app.ts
  - backend/src/modules/auth/auth.service.test.ts
  - backend/src/modules/auth/auth.middleware.test.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
user_setup: []
must_haves:
  truths:
    - Backend exposes working signup, login, logout, and current-user endpoints with email/password auth
    - Auth tokens are issued and cleared via the `auth_token` httpOnly cookie
    - Protected routes reject unauthenticated requests and expose authenticated user context to downstream handlers
  artifacts:
    - backend/src/modules/auth/auth.model.ts
    - backend/src/modules/auth/auth.service.ts
    - backend/src/modules/auth/auth.routes.ts
    - backend/src/middleware/require-auth.ts
    - backend/src/lib/jwt.ts
    - backend/src/modules/auth/auth.service.test.ts
  key_links:
    - auth routes are mounted from backend/src/app.ts under /api/auth
    - signup/login handlers call the auth service and set the auth_token cookie with httpOnly semantics
    - require-auth middleware verifies the same JWT payload shape issued by backend/src/lib/jwt.ts
---

<objective>
Implement the backend authentication domain, including user persistence, JWT cookie issuance, protected-route middleware, and the first auth-focused tests.

Purpose: Phase 1 only succeeds if the frontend can talk to a real auth API rather than mocked or placeholder handlers.
Output: `/api/auth/signup`, `/api/auth/login`, `/api/auth/logout`, `/api/auth/me`, user model, JWT helpers, middleware, and auth tests.
</objective>

<execution_context>
@/Users/satish/Desktop/AI-Powered Learning Assistant/.codex/get-shit-done/workflows/execute-plan.md
@/Users/satish/Desktop/AI-Powered Learning Assistant/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-authentication/01-CONTEXT.md
@.planning/phases/01-foundation-and-authentication/01-RESEARCH.md
@.planning/phases/01-foundation-and-authentication/01-VALIDATION.md
@backend/src/app.ts
@backend/src/server.ts
@backend/src/config/env.ts
@backend/src/config/db.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the auth domain model, schemas, and JWT helpers</name>
  <files>backend/src/modules/auth/auth.model.ts, backend/src/modules/auth/auth.schemas.ts, backend/src/lib/jwt.ts</files>
  <read_first>backend/src/config/env.ts, backend/src/app.ts, .planning/REQUIREMENTS.md, .planning/phases/01-foundation-and-authentication/01-CONTEXT.md</read_first>
  <action>Create `backend/src/modules/auth/auth.model.ts` with a Mongoose user schema containing `name`, `email`, and `passwordHash`, enforcing lowercase unique email storage. Create `backend/src/modules/auth/auth.schemas.ts` with Zod schemas for signup and login payloads using those exact field names. Create `backend/src/lib/jwt.ts` that signs and verifies a payload containing `userId` and `email`, using `JWT_SECRET` from validated env and the cookie name `auth_token` everywhere the token contract is referenced.</action>
  <verify>rg -n 'passwordHash|email:|name:' backend/src/modules/auth/auth.model.ts && rg -n 'signup|login|email|password' backend/src/modules/auth/auth.schemas.ts && rg -n 'auth_token|JWT_SECRET|userId' backend/src/lib/jwt.ts</verify>
  <acceptance_criteria>
    - `backend/src/modules/auth/auth.model.ts` contains `passwordHash`
    - `backend/src/modules/auth/auth.schemas.ts` contains both signup and login schema exports
    - `backend/src/lib/jwt.ts` contains `auth_token` and `userId`
  </acceptance_criteria>
  <done>Auth domain primitives exist and define a single source of truth for payloads and JWT handling.</done>
</task>

<task type="auto">
  <name>Task 2: Implement signup, login, logout, current-user routes, and protected middleware</name>
  <files>backend/src/modules/auth/auth.service.ts, backend/src/modules/auth/auth.controller.ts, backend/src/modules/auth/auth.routes.ts, backend/src/middleware/require-auth.ts, backend/src/app.ts</files>
  <read_first>backend/src/modules/auth/auth.model.ts, backend/src/modules/auth/auth.schemas.ts, backend/src/lib/jwt.ts, backend/src/app.ts, .planning/phases/01-foundation-and-authentication/01-CONTEXT.md</read_first>
  <action>Create an auth service that hashes passwords with `bcryptjs`, rejects duplicate emails, validates credentials, and returns a safe user object plus signed token. Implement controllers and routes under `/api/auth/signup`, `/api/auth/login`, `/api/auth/logout`, and `/api/auth/me`. Use `res.cookie('auth_token', token, { httpOnly: true, sameSite: 'lax', secure: process.env.NODE_ENV === 'production', path: '/' })` for login/signup responses and clear the same cookie on logout. Add `backend/src/middleware/require-auth.ts` that reads `req.cookies.auth_token`, verifies it, attaches auth context, and returns `401` with `Unauthorized` for missing/invalid tokens. Mount the auth router from `backend/src/app.ts`.</action>
  <verify>rg -n '/signup|/login|/logout|/me' backend/src/modules/auth/auth.routes.ts && rg -n 'httpOnly: true|sameSite: .lax.|auth_token' backend/src/modules/auth/auth.controller.ts backend/src/modules/auth/auth.service.ts && rg -n 'Unauthorized|req.cookies.auth_token' backend/src/middleware/require-auth.ts</verify>
  <acceptance_criteria>
    - `backend/src/modules/auth/auth.routes.ts` contains `/signup`, `/login`, `/logout`, and `/me`
    - login or signup response code sets `auth_token` with `httpOnly: true`
    - `backend/src/middleware/require-auth.ts` contains `Unauthorized`
    - `backend/src/app.ts` mounts the auth router under `/api/auth`
  </acceptance_criteria>
  <done>Backend exposes a real cookie-auth API and protected-route middleware that later frontend work can integrate with directly.</done>
</task>

<task type="auto">
  <name>Task 3: Add auth-focused tests and backend verification commands</name>
  <files>backend/src/modules/auth/auth.service.test.ts, backend/src/modules/auth/auth.middleware.test.ts, backend/package.json</files>
  <read_first>backend/vitest.config.ts, backend/src/test/setup.ts, backend/src/modules/auth/auth.service.ts, backend/src/middleware/require-auth.ts, .planning/phases/01-foundation-and-authentication/01-VALIDATION.md</read_first>
  <action>Create `backend/src/modules/auth/auth.service.test.ts` covering successful signup/login and invalid credential or duplicate-email failure paths. Create `backend/src/modules/auth/auth.middleware.test.ts` covering missing cookie and valid-token behavior. If needed, refine `backend/package.json` test scripts so `npm run test -- auth` or a similarly targeted auth test command executes without watch mode.</action>
  <verify>test -f backend/src/modules/auth/auth.service.test.ts && test -f backend/src/modules/auth/auth.middleware.test.ts && cd backend && npm run test -- auth</verify>
  <acceptance_criteria>
    - `backend/src/modules/auth/auth.service.test.ts` exists
    - `backend/src/modules/auth/auth.middleware.test.ts` exists
    - `backend/package.json` contains `test`
    - `cd backend && npm run test -- auth` exits 0
  </acceptance_criteria>
  <done>Auth logic has a repeatable automated safety net before the frontend integrates with it.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd backend && npm run test -- auth`
- [ ] `cd backend && npm run build`
- [ ] `rg -n '/api/auth|auth_token|Unauthorized' backend/src/app.ts backend/src/modules/auth backend/src/middleware/require-auth.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Signup, login, logout, and me endpoints exist and use cookie auth
- Protected middleware supports AUTH-03 without leaking unauthenticated access
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-authentication/01-02-SUMMARY.md`
</output>
