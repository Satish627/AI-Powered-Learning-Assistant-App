---
phase: 02-documents-and-reader
plan: 04
type: execute
wave: 4
depends_on:
  - "02-02"
  - "02-03"
files_modified:
  - frontend/package.json
  - frontend/bun.lock
  - frontend/app/(app)/library/[documentId]/page.tsx
  - frontend/components/reader/reader-workspace.tsx
  - frontend/components/reader/pdf-viewer.tsx
  - frontend/components/reader/document-companion-panel.tsx
  - frontend/components/library/document-delete-dialog.tsx
  - frontend/lib/documents.ts
autonomous: false
requirements:
  - DOC-04
  - READ-01
  - READ-02
  - READ-03
must_haves:
  truths:
    - Ready documents open into a dedicated `/library/[documentId]` study workspace instead of the browser PDF viewer
    - The embedded reader shows the PDF inside the app and supports previous/next page navigation
    - The workspace keeps document context and a reserved AI panel visible in the same flow per D-14 and READ-03
    - The workspace includes a clear route back to `Library` and reuses the real delete flow
  artifacts:
    - frontend/app/(app)/library/[documentId]/page.tsx
    - frontend/components/reader/reader-workspace.tsx
    - frontend/components/reader/pdf-viewer.tsx
    - frontend/components/reader/document-companion-panel.tsx
    - frontend/lib/documents.ts
  key_links:
    - `frontend/app/(app)/library/[documentId]/page.tsx` fetches both metadata and a signed access URL from the backend
    - `frontend/components/reader/pdf-viewer.tsx` configures `pdfjs.GlobalWorkerOptions.workerSrc` and binds page controls to the `react-pdf` `Document` and `Page` components
    - `frontend/components/reader/document-companion-panel.tsx` reserves the future AI area without changing the layout contract that Phase 3 will consume
---

<objective>
Add the document reader workspace: a protected route for opening ready documents, a client-only embedded PDF viewer with page controls, and the companion panel that keeps the reader inside the eventual AI study workflow.

Purpose: Phase 2 is incomplete until DOC-04 and READ-01 through READ-03 are satisfied in-browser, not just implied by a library button.
Output: `/library/[documentId]` route, `react-pdf` integration, page controls, companion panel, and a final human verification checkpoint for real browser behavior.
</objective>

<execution_context>
@/Users/satish/Desktop/AI-Powered Learning Assistant/.codex/get-shit-done/workflows/execute-plan.md
@/Users/satish/Desktop/AI-Powered Learning Assistant/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-documents-and-reader/02-CONTEXT.md
@.planning/phases/02-documents-and-reader/02-RESEARCH.md
@.planning/phases/02-documents-and-reader/02-VALIDATION.md
@.planning/phases/02-documents-and-reader/02-UI-SPEC.md
@AGENTS.md
@frontend/app/(app)/layout.tsx
@frontend/app/(app)/library/page.tsx
@frontend/components/library/library-screen.tsx
@frontend/components/library/document-delete-dialog.tsx
@frontend/lib/api.ts
@frontend/lib/documents.ts

<interfaces>
From `frontend/lib/documents.ts` after Plan 02-03:
```ts
export type DocumentStatus = "uploading" | "processing" | "ready" | "failed";
export interface StudyDocument { /* document metadata for library + reader */ }
export async function getDocumentsServer();
export async function deleteDocument(documentId: string);
```

This plan must add these helpers:
```ts
export async function getDocumentByIdServer(documentId: string);
export async function getDocumentAccessUrlServer(documentId: string);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the reader route and server-side document/access-url loading contract</name>
  <files>frontend/app/(app)/library/[documentId]/page.tsx, frontend/components/reader/reader-workspace.tsx, frontend/components/reader/document-companion-panel.tsx, frontend/lib/documents.ts, frontend/components/library/document-delete-dialog.tsx</files>
  <read_first>frontend/app/(app)/library/[documentId]/page.tsx, frontend/components/reader/reader-workspace.tsx, frontend/components/reader/document-companion-panel.tsx, frontend/components/library/document-delete-dialog.tsx, frontend/lib/documents.ts, frontend/app/(app)/layout.tsx, .planning/phases/02-documents-and-reader/02-CONTEXT.md, .planning/phases/02-documents-and-reader/02-UI-SPEC.md</read_first>
  <action>Extend `frontend/lib/documents.ts` with exact server helpers `getDocumentByIdServer(documentId: string)` and `getDocumentAccessUrlServer(documentId: string)` using `serverApiFetch` against `/api/documents/${documentId}` and `/api/documents/${documentId}/access-url`. Create `frontend/app/(app)/library/[documentId]/page.tsx` as a server component that loads metadata first, then loads a signed access URL only when `document.status === "ready"`. If the document is not ready, render a blocked-state card with the exact status-aware copy `This document isn't ready to open yet.` and a `Back to Library` link instead of attempting to render the PDF. When the document is ready, render `ReaderWorkspace` with a header area containing `Back to Library`, the document title, the current status badge, and metadata rows for filename, size, upload date, and page count per D-15 and D-16. Build `document-companion-panel.tsx` so the right-side panel shows those metadata rows, a reuse of the existing delete dialog trigger, and a reserved AI section labeled exactly `AI study tools` with supporting copy `Coming next in Phase 3.` per READ-03. Wire the reader delete trigger through an explicit success callback such as `onDeleted={() => router.push("/library")}` or equivalent so deleting from the reader route always returns the user to `/library`.</action>
  <verify>
    <automated>rg -n 'getDocumentByIdServer|getDocumentAccessUrlServer|/access-url' frontend/lib/documents.ts && rg -n 'Back to Library|This document isn.t ready to open yet\\.|AI study tools|Coming next in Phase 3\\.' frontend/app/'(app)'/library/'[documentId]'/page.tsx frontend/components/reader/reader-workspace.tsx frontend/components/reader/document-companion-panel.tsx && bun --cwd frontend run build</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/lib/documents.ts` contains `getDocumentByIdServer` and `getDocumentAccessUrlServer`
    - `frontend/app/(app)/library/[documentId]/page.tsx` contains `Back to Library`
    - `frontend/app/(app)/library/[documentId]/page.tsx` contains `This document isn't ready to open yet.`
    - `frontend/components/reader/document-companion-panel.tsx` contains `AI study tools`
    - `frontend/components/reader/document-companion-panel.tsx` contains `Coming next in Phase 3.`
    - `frontend/components/reader/reader-workspace.tsx` or `frontend/app/(app)/library/[documentId]/page.tsx` contains `router.push("/library")` or an equivalent explicit post-delete redirect
  </acceptance_criteria>
  <done>The study workspace route exists, handles non-ready documents safely, and reserves the right-side AI panel without forcing a Phase 3 layout reset.</done>
</task>

<task type="auto">
  <name>Task 2: Add the client-only embedded PDF viewer and page navigation controls</name>
  <files>frontend/package.json, frontend/bun.lock, frontend/components/reader/pdf-viewer.tsx, frontend/components/reader/reader-workspace.tsx</files>
  <read_first>frontend/package.json, frontend/bun.lock, frontend/components/reader/pdf-viewer.tsx, frontend/components/reader/reader-workspace.tsx, frontend/components/ui/button.tsx, .planning/phases/02-documents-and-reader/02-RESEARCH.md, .planning/phases/02-documents-and-reader/02-UI-SPEC.md</read_first>
  <action>Install `react-pdf` in `frontend/package.json` and update `frontend/bun.lock`. Create `frontend/components/reader/pdf-viewer.tsx` as a client component that configures `pdfjs.GlobalWorkerOptions.workerSrc = new URL("pdfjs-dist/build/pdf.worker.min.mjs", import.meta.url).toString();`, renders a `Document` with one `Page` at a time, and exposes page state with exact control labels `Previous page` and `Next page`. Build `reader-workspace.tsx` so the desktop layout uses a dominant main pane plus a `380px` companion panel per the UI spec, while mobile stacks the viewer first and companion content below. Disable the previous button on page `1` and disable the next button on the last page. Keep the current page label in the exact format `Page {current} of {total}` and show a framed viewer surface inside the app rather than linking out to the browser PDF tab per D-13 and READ-02.</action>
  <verify>
    <automated>rg -n 'react-pdf' frontend/package.json && rg -n 'pdfjs\\.GlobalWorkerOptions\\.workerSrc|Document|Page|Previous page|Next page|Page \\{current\\} of \\{total\\}' frontend/components/reader/pdf-viewer.tsx frontend/components/reader/reader-workspace.tsx && bun --cwd frontend run build</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/package.json` contains `react-pdf`
    - `frontend/components/reader/pdf-viewer.tsx` contains `pdfjs.GlobalWorkerOptions.workerSrc`
    - `frontend/components/reader/pdf-viewer.tsx` contains `Previous page` and `Next page`
    - `frontend/components/reader/reader-workspace.tsx` contains `Page {current} of {total}` or equivalent exact page-label template
    - `bun --cwd frontend run build` exits `0`
  </acceptance_criteria>
  <done>The reader renders PDFs inside the app with client-only PDF.js wiring and accessible previous/next page controls.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the reader workflow in a real browser</name>
  <files>frontend/app/(app)/library/[documentId]/page.tsx, frontend/components/reader/reader-workspace.tsx, frontend/components/reader/pdf-viewer.tsx, frontend/components/reader/document-companion-panel.tsx, frontend/components/library/document-delete-dialog.tsx</files>
  <read_first>frontend/app/(app)/library/[documentId]/page.tsx, frontend/components/reader/reader-workspace.tsx, frontend/components/reader/pdf-viewer.tsx, frontend/components/reader/document-companion-panel.tsx, .planning/phases/02-documents-and-reader/02-UI-SPEC.md, .planning/phases/02-documents-and-reader/02-VALIDATION.md</read_first>
  <action>Use the already-built reader route and components to run the final browser checkpoint for READ-01 through READ-03. Do not add new scope during this checkpoint; only confirm the implemented route, embedded PDF renderer, page controls, companion panel, back navigation, and delete return path behave exactly as specified before Phase 2 is considered execution-complete.</action>
  <what-built>Protected reader workspace with embedded PDF rendering, page controls, metadata companion panel, back navigation, and delete reuse.</what-built>
  <how-to-verify>
    1. Start the backend and frontend apps with the phase environment configured for object storage.
    2. Log in through `/auth` and open `/library`.
    3. Upload a multi-page PDF and wait until its status becomes `ready`.
    4. Click `Open` on that document and confirm the route is `/library/{documentId}` rather than a browser PDF tab.
    5. Confirm the PDF renders inside the main pane and the header shows `Back to Library`, the document title, and the status badge.
    6. Click `Next page`, then `Previous page`, and confirm the label updates in the `Page {current} of {total}` format while the buttons disable at the bounds.
    7. Confirm the companion panel shows metadata plus the `AI study tools` / `Coming next in Phase 3.` placeholder.
    8. Use the delete action from the reader and confirm the app returns to `/library` with the document removed.
  </how-to-verify>
  <verify>
    <automated>npm --prefix backend test -- src/modules/documents/documents.routes.test.ts && bun --cwd frontend run build</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/app/(app)/library/[documentId]/page.tsx` contains `Back to Library`
    - `frontend/components/reader/pdf-viewer.tsx` contains `Previous page` and `Next page`
    - `frontend/components/reader/document-companion-panel.tsx` contains `AI study tools`
    - `bun --cwd frontend run build` exits `0`
    - human verification confirms the PDF renders, page navigation works, and delete returns to `/library`
  </acceptance_criteria>
  <done>The browser checkpoint is either approved for Phase 2 completion or returns a precise failing step that can be addressed in follow-up work.</done>
  <resume-signal>Type `approved` if the browser flow matches the checklist, or report the exact failing step.</resume-signal>
</task>

</tasks>

<verification>
- [ ] `bun --cwd frontend run build`
- [ ] `npm --prefix backend test -- src/modules/documents/documents.routes.test.ts`
- [ ] Manual browser verification confirms open, render, page navigation, back navigation, and delete from the reader
</verification>

<success_criteria>
Ready documents open into an in-app study workspace with embedded PDF rendering, working page controls, a preserved AI-ready side panel, and a verified back/delete flow.
</success_criteria>

<output>
After completion, create `.planning/phases/02-documents-and-reader/02-04-SUMMARY.md`
</output>
