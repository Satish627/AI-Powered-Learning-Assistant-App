---
phase: 02-documents-and-reader
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - backend/src/app.ts
  - backend/src/modules/documents/documents.service.ts
  - backend/src/modules/documents/documents.controller.ts
  - backend/src/modules/documents/documents.routes.ts
  - backend/src/modules/documents/documents.service.test.ts
  - backend/src/modules/documents/documents.routes.test.ts
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03
  - DOC-04
  - DOC-05
user_setup:
  - Provision one S3-compatible bucket and set `STORAGE_BUCKET`, `STORAGE_REGION`, `STORAGE_ENDPOINT`, `STORAGE_ACCESS_KEY_ID`, `STORAGE_SECRET_ACCESS_KEY`, and `STORAGE_FORCE_PATH_STYLE` before live upload testing.
must_haves:
  truths:
    - Authenticated users can initialize a single PDF upload without sending file bytes through Express
    - `POST /api/documents/:documentId/finalize` returns a document in `processing` first, then asynchronous metadata work settles it to `ready` or `failed`
    - Only the owning user can list, inspect, finalize, mint a read URL for, or delete a document
    - Deleting a document removes both object-storage state and MongoDB metadata per D-04
  artifacts:
    - backend/src/modules/documents/documents.service.ts
    - backend/src/modules/documents/documents.controller.ts
    - backend/src/modules/documents/documents.routes.ts
    - backend/src/modules/documents/documents.service.test.ts
    - backend/src/modules/documents/documents.routes.test.ts
  key_links:
    - `/api/documents` creates the MongoDB record before upload and returns a presigned `PUT` URL
    - `/api/documents/:documentId/finalize` persists `status: "processing"` immediately and triggers asynchronous metadata extraction so the UI can observe `processing`
    - `/api/documents/:documentId/access-url` only returns a signed viewer URL when the document status is `ready`
    - `DELETE /api/documents/:documentId` deletes the storage object and the document record for the authenticated owner
---

<objective>
Build the backend document lifecycle API for Phase 2: owner-scoped init/finalize/list/get/access/delete routes, asynchronous post-upload processing, and passing backend tests.

Purpose: This plan turns the Phase 2 backend foundation into the concrete contract the library and reader UI will call, while explicitly making the `processing` state observable so DOC-02 and D-06 are actually deliverable.
Output: Auth-protected document service/controller/routes, asynchronous finalize workflow, and passing backend service/route tests.
</objective>

<execution_context>
@/Users/satish/Desktop/AI-Powered Learning Assistant/.codex/get-shit-done/workflows/execute-plan.md
@/Users/satish/Desktop/AI-Powered Learning Assistant/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-documents-and-reader/02-CONTEXT.md
@.planning/phases/02-documents-and-reader/02-RESEARCH.md
@.planning/phases/02-documents-and-reader/02-VALIDATION.md
@AGENTS.md
@backend/src/app.ts
@backend/src/lib/jwt.ts
@backend/src/middleware/require-auth.ts
@backend/src/modules/documents/documents.model.ts
@backend/src/modules/documents/documents.schemas.ts
@backend/src/modules/documents/documents.storage.ts
@backend/src/modules/documents/documents.pdf.ts

<interfaces>
From `backend/src/lib/jwt.ts`:
```ts
export const AUTH_COOKIE_NAME = "auth_token";
export interface AuthTokenPayload {
  userId: string;
  email: string;
}
```

From `backend/src/middleware/require-auth.ts`:
```ts
export type AuthenticatedRequest = Request & {
  auth: AuthTokenPayload;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the document service/controller/routes with observable processing state</name>
  <files>backend/src/app.ts, backend/src/modules/documents/documents.service.ts, backend/src/modules/documents/documents.controller.ts, backend/src/modules/documents/documents.routes.ts</files>
  <read_first>backend/src/app.ts, backend/src/lib/jwt.ts, backend/src/middleware/require-auth.ts, backend/src/modules/documents/documents.model.ts, backend/src/modules/documents/documents.schemas.ts, backend/src/modules/documents/documents.storage.ts, backend/src/modules/documents/documents.pdf.ts, backend/src/modules/documents/documents.service.ts, backend/src/modules/documents/documents.controller.ts, backend/src/modules/documents/documents.routes.ts, .planning/REQUIREMENTS.md, .planning/phases/02-documents-and-reader/02-CONTEXT.md, .planning/phases/02-documents-and-reader/02-RESEARCH.md</read_first>
  <action>Create `documents.service.ts` and `documents.controller.ts` to support these exact routes behind `requireAuth`: `POST /api/documents` accepts `{ fileName, sizeBytes, contentType }`, rejects non-`application/pdf`, creates a record with `status: "uploading"` and `processing.stage: "uploaded"`, stores object keys as `documents/${userId}/${documentId}.pdf`, and returns `{ document, uploadUrl, uploadMethod: "PUT", uploadHeaders: { "Content-Type": "application/pdf" } }`; `POST /api/documents/:documentId/finalize` verifies the object with `HEAD`, updates the document immediately to `status: "processing"` and `processing.stage: "metadata_extraction"`, returns that `processing` document in the HTTP response, and then triggers asynchronous metadata extraction in a detached function such as `void continueDocumentPreparation(documentId)` or `setImmediate(() => continueDocumentPreparation(documentId))` that later persists `status: "ready"` with metadata or `status: "failed"` with `processing.errorCode` and `processing.errorMessage`; `GET /api/documents` returns only owner documents sorted by newest first; `GET /api/documents/:documentId` returns owner-scoped metadata for polling and reader loading; `GET /api/documents/:documentId/access-url` returns `{ url, expiresAt }` only when `status === "ready"`; `DELETE /api/documents/:documentId` deletes the object and metadata, returning `204` even when the storage object is already missing but the owned document record exists. Mount the router at `app.use("/api/documents", documentsRouter)` in `backend/src/app.ts`.</action>
  <verify>
    <automated>rg -n 'processing.stage|continueDocumentPreparation|setImmediate|/finalize|/access-url' backend/src/modules/documents/documents.service.ts backend/src/modules/documents/documents.controller.ts backend/src/modules/documents/documents.routes.ts && rg -n 'app.use\\(\"/api/documents\", documentsRouter\\)' backend/src/app.ts && npm --prefix backend run build</automated>
  </verify>
  <acceptance_criteria>
    - `backend/src/modules/documents/documents.service.ts` contains `status: "processing"` or equivalent immediate processing-state persistence during finalize
    - `backend/src/modules/documents/documents.service.ts` contains `continueDocumentPreparation` or equivalent asynchronous follow-up helper
    - `backend/src/modules/documents/documents.routes.ts` contains `/finalize` and `/access-url`
    - `backend/src/modules/documents/documents.service.ts` contains `documents/${userId}/${documentId}.pdf`
    - `backend/src/app.ts` contains `app.use("/api/documents", documentsRouter)`
  </acceptance_criteria>
  <done>The backend lifecycle API exists and explicitly exposes a visible `processing` state instead of jumping directly from upload to `ready`.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the Wave 0 placeholders with passing lifecycle and route tests</name>
  <files>backend/src/modules/documents/documents.service.test.ts, backend/src/modules/documents/documents.routes.test.ts</files>
  <read_first>backend/vitest.config.ts, backend/src/test/setup.ts, backend/src/modules/documents/documents.service.ts, backend/src/modules/documents/documents.controller.ts, backend/src/modules/documents/documents.routes.ts, backend/src/modules/documents/documents.storage.ts, backend/src/modules/documents/documents.pdf.ts, backend/src/modules/documents/documents.service.test.ts, backend/src/modules/documents/documents.routes.test.ts, .planning/phases/02-documents-and-reader/02-VALIDATION.md</read_first>
  <action>Turn the Wave 0 test files into passing tests. In `documents.service.test.ts`, mock the storage adapter and PDF metadata helper so the service cases assert these exact transitions: `uploading -> processing`, asynchronous completion to `ready`, and finalize failure transitions to `failed`, plus metadata persistence for title and page count. In `documents.routes.test.ts`, use `supertest` against `backend/src/app.ts` with mocked auth and storage helpers to assert these exact responses: `POST /api/documents` returns `201` and an `uploadUrl`; `POST /api/documents/:documentId/finalize` returns a document with `status` equal to `processing`; `GET /api/documents` excludes documents from another owner; `GET /api/documents/:documentId/access-url` returns `409` or `400` for non-ready documents and `200` for ready documents; `DELETE /api/documents/:documentId` returns `204` and verifies both storage delete and metadata delete were called.</action>
  <verify>
    <automated>npm --prefix backend test -- src/modules/documents/documents.service.test.ts src/modules/documents/documents.routes.test.ts && npm --prefix backend run build</automated>
  </verify>
  <acceptance_criteria>
    - `backend/src/modules/documents/documents.service.test.ts` contains `uploading` and `processing`
    - `backend/src/modules/documents/documents.service.test.ts` contains `ready` and `failed`
    - `backend/src/modules/documents/documents.routes.test.ts` contains `/finalize`
    - `backend/src/modules/documents/documents.routes.test.ts` contains `processing`
    - `npm --prefix backend test -- src/modules/documents/documents.service.test.ts src/modules/documents/documents.routes.test.ts` exits `0`
    - `npm --prefix backend run build` exits `0`
  </acceptance_criteria>
  <done>Phase 2 backend behavior is covered by repeatable automated tests, including the visible `processing` state the frontend library will depend on.</done>
</task>

</tasks>

<verification>
- [ ] `npm --prefix backend test -- src/modules/documents/documents.service.test.ts src/modules/documents/documents.routes.test.ts`
- [ ] `npm --prefix backend run build`
- [ ] `rg -n 'processing|continueDocumentPreparation|/finalize|/access-url' backend/src/modules/documents`
</verification>

<success_criteria>
The backend can create, finalize, list, access, and delete owned PDF documents through auth-protected routes, and finalize exposes a real `processing` state before the document settles to `ready` or `failed`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-documents-and-reader/02-02-SUMMARY.md`
</output>
