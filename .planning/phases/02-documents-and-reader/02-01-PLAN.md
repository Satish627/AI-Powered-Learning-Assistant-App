---
phase: 02-documents-and-reader
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/package.json
  - backend/package-lock.json
  - backend/.env.example
  - backend/src/config/env.ts
  - backend/src/test/setup.ts
  - backend/src/app.ts
  - backend/src/modules/documents/documents.model.ts
  - backend/src/modules/documents/documents.schemas.ts
  - backend/src/modules/documents/documents.storage.ts
  - backend/src/modules/documents/documents.pdf.ts
  - backend/src/modules/documents/documents.service.ts
  - backend/src/modules/documents/documents.controller.ts
  - backend/src/modules/documents/documents.routes.ts
  - backend/src/modules/documents/documents.service.test.ts
  - backend/src/modules/documents/documents.routes.test.ts
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03
  - DOC-04
  - DOC-05
user_setup:
  - Provision one S3-compatible bucket and set `STORAGE_BUCKET`, `STORAGE_REGION`, `STORAGE_ENDPOINT`, `STORAGE_ACCESS_KEY_ID`, `STORAGE_SECRET_ACCESS_KEY`, and `STORAGE_FORCE_PATH_STYLE` before live upload testing.
must_haves:
  truths:
    - Authenticated users can initialize a single PDF upload without sending file bytes through Express
    - Document records persist owner, metadata, and the Phase 2 lifecycle states `uploading`, `processing`, `ready`, and `failed`
    - Only the owning user can list, inspect, finalize, mint a read URL for, or delete a document
    - Deleting a document removes both object-storage state and MongoDB metadata per D-04
  artifacts:
    - backend/src/modules/documents/documents.model.ts
    - backend/src/modules/documents/documents.storage.ts
    - backend/src/modules/documents/documents.service.ts
    - backend/src/modules/documents/documents.controller.ts
    - backend/src/modules/documents/documents.routes.ts
    - backend/src/modules/documents/documents.service.test.ts
    - backend/src/modules/documents/documents.routes.test.ts
  key_links:
    - `/api/documents` creates the MongoDB record before upload and returns a presigned `PUT` URL
    - `/api/documents/:documentId/finalize` verifies the uploaded object, extracts PDF metadata, and transitions status to `ready` or `failed`
    - `/api/documents/:documentId/access-url` only returns a signed viewer URL when the document status is `ready`
    - `DELETE /api/documents/:documentId` deletes the storage object and the document record for the authenticated owner
---

<objective>
Build the backend document lifecycle for Phase 2: durable object storage integration, owner-scoped document records, and the upload/list/open/delete API surface the frontend will depend on.

Purpose: Phase 2 cannot ship as a local-file demo. The executor needs a production-shaped backend contract that satisfies D-01 through D-08 before any library or reader UI work is layered on top.
Output: Document model, S3-compatible storage adapter, metadata extraction helper, auth-protected document routes, and passing backend tests for upload/init/finalize/list/access/delete behavior.
</objective>

<execution_context>
@/Users/satish/Desktop/AI-Powered Learning Assistant/.codex/get-shit-done/workflows/execute-plan.md
@/Users/satish/Desktop/AI-Powered Learning Assistant/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-documents-and-reader/02-CONTEXT.md
@.planning/phases/02-documents-and-reader/02-RESEARCH.md
@.planning/phases/02-documents-and-reader/02-VALIDATION.md
@AGENTS.md
@backend/src/app.ts
@backend/src/config/env.ts
@backend/src/lib/jwt.ts
@backend/src/middleware/require-auth.ts
@backend/src/modules/auth/auth.model.ts

<interfaces>
From `backend/src/lib/jwt.ts`:
```ts
export const AUTH_COOKIE_NAME = "auth_token";
export interface AuthTokenPayload {
  userId: string;
  email: string;
}
```

From `backend/src/middleware/require-auth.ts`:
```ts
export type AuthenticatedRequest = Request & {
  auth: AuthTokenPayload;
};
```

From `backend/src/config/env.ts`:
```ts
export const env = envSchema.parse(process.env);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the storage/env contract and Wave 0 document test scaffolds</name>
  <files>backend/package.json, backend/package-lock.json, backend/.env.example, backend/src/config/env.ts, backend/src/test/setup.ts, backend/src/modules/documents/documents.service.test.ts, backend/src/modules/documents/documents.routes.test.ts</files>
  <read_first>backend/package.json, backend/.env.example, backend/src/config/env.ts, backend/src/test/setup.ts, backend/src/modules/documents/documents.service.test.ts, backend/src/modules/documents/documents.routes.test.ts, .planning/phases/02-documents-and-reader/02-CONTEXT.md, .planning/phases/02-documents-and-reader/02-RESEARCH.md, .planning/phases/02-documents-and-reader/02-VALIDATION.md, AGENTS.md</read_first>
  <action>Install `@aws-sdk/client-s3`, `@aws-sdk/s3-request-presigner`, and `pdfjs-dist` in `backend/package.json`, and install `supertest` as a backend dev dependency exactly as researched. Extend `backend/src/config/env.ts`, `backend/.env.example`, and `backend/src/test/setup.ts` with the exact storage keys `STORAGE_BUCKET`, `STORAGE_REGION`, `STORAGE_ENDPOINT`, `STORAGE_ACCESS_KEY_ID`, `STORAGE_SECRET_ACCESS_KEY`, and `STORAGE_FORCE_PATH_STYLE`. Create Wave 0 test files at `backend/src/modules/documents/documents.service.test.ts` and `backend/src/modules/documents/documents.routes.test.ts` with concrete case names for D-03 ownership enforcement and D-06 lifecycle states: `initializes a PDF upload`, `lists only owner documents`, `returns a read URL only for ready docs`, `deletes storage and metadata`, `persists metadata and status`, and `marks finalize failures as failed`.</action>
  <verify>
    <automated>test -f backend/src/modules/documents/documents.service.test.ts && test -f backend/src/modules/documents/documents.routes.test.ts && rg -n 'STORAGE_BUCKET|STORAGE_REGION|STORAGE_ENDPOINT|STORAGE_ACCESS_KEY_ID|STORAGE_SECRET_ACCESS_KEY|STORAGE_FORCE_PATH_STYLE' backend/.env.example backend/src/config/env.ts backend/src/test/setup.ts && rg -n '@aws-sdk/client-s3|@aws-sdk/s3-request-presigner|pdfjs-dist|supertest' backend/package.json</automated>
  </verify>
  <acceptance_criteria>
    - `backend/.env.example` contains `STORAGE_BUCKET=`, `STORAGE_REGION=`, `STORAGE_ENDPOINT=`, `STORAGE_ACCESS_KEY_ID=`, `STORAGE_SECRET_ACCESS_KEY=`, and `STORAGE_FORCE_PATH_STYLE=`
    - `backend/src/test/setup.ts` contains defaults for the same six storage variables
    - `backend/src/modules/documents/documents.routes.test.ts` contains `initializes a PDF upload` and `deletes storage and metadata`
    - `backend/src/modules/documents/documents.service.test.ts` contains `persists metadata and status` and `marks finalize failures as failed`
    - `backend/package.json` contains `@aws-sdk/client-s3`, `@aws-sdk/s3-request-presigner`, `pdfjs-dist`, and `supertest`
  </acceptance_criteria>
  <done>Storage configuration is explicit and the backend test files exist with concrete document-lifecycle case names before implementation proceeds.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the document model, storage adapter, lifecycle service, and auth-protected routes</name>
  <files>backend/src/app.ts, backend/src/modules/documents/documents.model.ts, backend/src/modules/documents/documents.schemas.ts, backend/src/modules/documents/documents.storage.ts, backend/src/modules/documents/documents.pdf.ts, backend/src/modules/documents/documents.service.ts, backend/src/modules/documents/documents.controller.ts, backend/src/modules/documents/documents.routes.ts</files>
  <read_first>backend/src/app.ts, backend/src/lib/jwt.ts, backend/src/middleware/require-auth.ts, backend/src/modules/auth/auth.model.ts, backend/src/modules/documents/documents.model.ts, backend/src/modules/documents/documents.schemas.ts, backend/src/modules/documents/documents.storage.ts, backend/src/modules/documents/documents.pdf.ts, backend/src/modules/documents/documents.service.ts, backend/src/modules/documents/documents.controller.ts, backend/src/modules/documents/documents.routes.ts, .planning/REQUIREMENTS.md, .planning/phases/02-documents-and-reader/02-CONTEXT.md, .planning/phases/02-documents-and-reader/02-RESEARCH.md</read_first>
  <action>Create `backend/src/modules/documents/documents.model.ts` with a Mongoose schema keyed by `ownerId`, `title`, `originalFilename`, `mimeType`, `sizeBytes`, `pageCount`, `status`, `processing.stage`, `processing.errorCode`, `processing.errorMessage`, `storage.provider`, `storage.bucket`, `storage.key`, `storage.etag`, `createdAt`, and `updatedAt`. Use the exact user-facing status enum `uploading | processing | ready | failed` per D-06 and a separate internal processing stage enum that includes `uploaded`, `metadata_extraction`, `text_extraction`, `chunking`, `embedding`, `complete`, and `failed` per D-08. Create `documents.storage.ts` with four exact helpers: `createDocumentUploadUrl`, `headDocumentObject`, `createDocumentAccessUrl`, and `deleteDocumentObject`, using `S3Client`, `PutObjectCommand`, `GetObjectCommand`, `DeleteObjectCommand`, `HeadObjectCommand`, and `getSignedUrl`; do not proxy PDF bytes through Express. Create `documents.pdf.ts` that reads the uploaded PDF from object storage bytes with `pdfjs-dist` and returns `{ pageCount, derivedTitle }`, falling back to the original filename when the embedded title is blank. Create `documents.service.ts` and `documents.controller.ts` to support these exact routes behind `requireAuth`: `POST /api/documents` accepts `{ fileName, sizeBytes, contentType }`, rejects non-`application/pdf`, creates a record with `status: "uploading"` and `processing.stage: "uploaded"`, stores object keys as `documents/${userId}/${documentId}.pdf`, and returns `{ document, uploadUrl, uploadMethod: "PUT", uploadHeaders: { "Content-Type": "application/pdf" } }`; `POST /api/documents/:documentId/finalize` verifies the object with `HEAD`, transitions through `processing`, extracts metadata, then persists `status: "ready"` or `status: "failed"`; `GET /api/documents` returns only owner documents sorted by newest first; `GET /api/documents/:documentId` returns owner-scoped metadata; `GET /api/documents/:documentId/access-url` returns `{ url, expiresAt }` only when `status === "ready"`; `DELETE /api/documents/:documentId` deletes the object and metadata, returning `204` even when the storage object is already missing but the owned document record exists per D-04. Mount the router at `app.use("/api/documents", documentsRouter)` in `backend/src/app.ts`.</action>
  <verify>
    <automated>rg -n 'uploading|processing|ready|failed|metadata_extraction|text_extraction|chunking|embedding|complete' backend/src/modules/documents/documents.model.ts && rg -n 'createDocumentUploadUrl|headDocumentObject|createDocumentAccessUrl|deleteDocumentObject|PutObjectCommand|GetObjectCommand|DeleteObjectCommand|HeadObjectCommand' backend/src/modules/documents/documents.storage.ts && rg -n 'POST /api/documents|/finalize|/access-url|DELETE' backend/src/modules/documents/documents.routes.ts && npm --prefix backend run build</automated>
  </verify>
  <acceptance_criteria>
    - `backend/src/modules/documents/documents.model.ts` contains `status` with `uploading`, `processing`, `ready`, and `failed`
    - `backend/src/modules/documents/documents.model.ts` contains `processing.stage`
    - `backend/src/modules/documents/documents.storage.ts` contains the four helper names `createDocumentUploadUrl`, `headDocumentObject`, `createDocumentAccessUrl`, and `deleteDocumentObject`
    - `backend/src/modules/documents/documents.service.ts` contains `documents/${userId}/${documentId}.pdf`
    - `backend/src/modules/documents/documents.routes.ts` contains `/finalize` and `/access-url`
    - `backend/src/app.ts` contains `app.use("/api/documents", documentsRouter)`
  </acceptance_criteria>
  <done>The backend exposes a production-shaped document lifecycle API that matches the Phase 2 storage, status, and ownership decisions.</done>
</task>

<task type="auto">
  <name>Task 3: Replace the Wave 0 placeholders with passing service and route tests</name>
  <files>backend/src/modules/documents/documents.service.test.ts, backend/src/modules/documents/documents.routes.test.ts, backend/package.json</files>
  <read_first>backend/package.json, backend/vitest.config.ts, backend/src/test/setup.ts, backend/src/modules/documents/documents.service.ts, backend/src/modules/documents/documents.routes.ts, backend/src/modules/documents/documents.controller.ts, backend/src/modules/documents/documents.storage.ts, backend/src/modules/documents/documents.pdf.ts, backend/src/modules/documents/documents.service.test.ts, backend/src/modules/documents/documents.routes.test.ts, .planning/phases/02-documents-and-reader/02-VALIDATION.md</read_first>
  <action>Turn the Task 1 test scaffolds into passing tests. In `documents.service.test.ts`, mock the storage adapter and PDF metadata helper so the service cases assert exact status transitions for `uploading -> processing -> ready` and `uploading -> failed`, plus derived metadata persistence for title and page count. In `documents.routes.test.ts`, use `supertest` against `backend/src/app.ts` with mocked auth and storage helpers to assert these exact responses: `POST /api/documents` returns `201` and an `uploadUrl`; `GET /api/documents` excludes documents from another owner; `GET /api/documents/:documentId/access-url` returns `409` or `400` for non-ready documents and `200` for ready documents; `DELETE /api/documents/:documentId` returns `204` and verifies both storage delete and metadata delete were called. Keep the backend test script non-watch and runnable via targeted path arguments.</action>
  <verify>
    <automated>npm --prefix backend test -- src/modules/documents/documents.service.test.ts src/modules/documents/documents.routes.test.ts && npm --prefix backend run build</automated>
  </verify>
  <acceptance_criteria>
    - `backend/src/modules/documents/documents.service.test.ts` contains `uploading -> processing -> ready` or equivalent assertions for both `processing` and `ready`
    - `backend/src/modules/documents/documents.service.test.ts` contains `failed`
    - `backend/src/modules/documents/documents.routes.test.ts` contains `POST /api/documents`, `/access-url`, and `204`
    - `npm --prefix backend test -- src/modules/documents/documents.service.test.ts src/modules/documents/documents.routes.test.ts` exits `0`
    - `npm --prefix backend run build` exits `0`
  </acceptance_criteria>
  <done>Phase 2 backend behavior is covered by repeatable automated tests before any frontend document work starts.</done>
</task>

</tasks>

<verification>
- [ ] `npm --prefix backend test -- src/modules/documents/documents.service.test.ts src/modules/documents/documents.routes.test.ts`
- [ ] `npm --prefix backend run build`
- [ ] `rg -n 'app.use\\("/api/documents"|uploading|processing|ready|failed|createDocumentUploadUrl' backend/src/app.ts backend/src/modules/documents`
</verification>

<success_criteria>
The backend can create, finalize, list, access, and delete owned PDF documents through auth-protected routes without storing PDFs on local disk, and the targeted document tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/02-documents-and-reader/02-01-SUMMARY.md`
</output>
