---
phase: 02-documents-and-reader
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/package.json
  - backend/package-lock.json
  - backend/.env.example
  - backend/src/config/env.ts
  - backend/src/test/setup.ts
  - backend/src/modules/documents/documents.service.test.ts
  - backend/src/modules/documents/documents.routes.test.ts
  - backend/src/modules/documents/documents.model.ts
  - backend/src/modules/documents/documents.schemas.ts
  - backend/src/modules/documents/documents.storage.ts
  - backend/src/modules/documents/documents.pdf.ts
autonomous: true
requirements:
  - DOC-01
  - DOC-02
user_setup:
  - Provision one S3-compatible bucket and set `STORAGE_BUCKET`, `STORAGE_REGION`, `STORAGE_ENDPOINT`, `STORAGE_ACCESS_KEY_ID`, `STORAGE_SECRET_ACCESS_KEY`, and `STORAGE_FORCE_PATH_STYLE` before live upload testing.
must_haves:
  truths:
    - Phase 2 has a durable storage contract and document schema that can persist owner-scoped PDF metadata without relying on local disk
    - The document model can represent both user-visible lifecycle state and a deeper processing pipeline without a Phase 3 schema redesign per D-08
    - The backend foundation is ready for an observable `uploading -> processing -> ready|failed` lifecycle instead of a single boolean processed flag
    - The backend foundation already defines the test surface the Phase 2 lifecycle API will use, so the next plan can verify upload/list/access/delete behavior with repeatable backend checks
  artifacts:
    - backend/src/modules/documents/documents.model.ts
    - backend/src/modules/documents/documents.schemas.ts
    - backend/src/modules/documents/documents.storage.ts
    - backend/src/modules/documents/documents.pdf.ts
    - backend/src/modules/documents/documents.service.test.ts
    - backend/src/modules/documents/documents.routes.test.ts
  key_links:
    - `backend/src/config/env.ts` exposes the exact storage env keys the later lifecycle routes will require
    - `backend/src/modules/documents/documents.model.ts` separates the user-facing `status` field from `processing.stage`
    - `backend/src/modules/documents/documents.storage.ts` contains the presigned upload/read and storage-inspection helpers that later routes will call
---

<objective>
Build the backend foundation for Phase 2: dependencies, storage/env contract, document schema, storage adapter, PDF metadata helper, and Wave 0 document test scaffolds.

Purpose: This keeps the most infrastructure-heavy backend work isolated before the lifecycle API and tests are layered on top. It reduces execution risk and keeps the backend file budget below the checker threshold.
Output: Backend storage configuration, document model/schema, storage helpers, PDF helper, and placeholder test files aligned to the Nyquist validation contract.
</objective>

<execution_context>
@/Users/satish/Desktop/AI-Powered Learning Assistant/.codex/get-shit-done/workflows/execute-plan.md
@/Users/satish/Desktop/AI-Powered Learning Assistant/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-documents-and-reader/02-CONTEXT.md
@.planning/phases/02-documents-and-reader/02-RESEARCH.md
@.planning/phases/02-documents-and-reader/02-VALIDATION.md
@AGENTS.md
@backend/src/config/env.ts
@backend/src/test/setup.ts
@backend/src/modules/auth/auth.model.ts

<interfaces>
From `backend/src/config/env.ts`:
```ts
export const env = envSchema.parse(process.env);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add storage dependencies, env keys, and Wave 0 document test scaffolds</name>
  <files>backend/package.json, backend/package-lock.json, backend/.env.example, backend/src/config/env.ts, backend/src/test/setup.ts, backend/src/modules/documents/documents.service.test.ts, backend/src/modules/documents/documents.routes.test.ts</files>
  <read_first>backend/package.json, backend/.env.example, backend/src/config/env.ts, backend/src/test/setup.ts, .planning/phases/02-documents-and-reader/02-RESEARCH.md, .planning/phases/02-documents-and-reader/02-VALIDATION.md, AGENTS.md</read_first>
  <action>Install `@aws-sdk/client-s3`, `@aws-sdk/s3-request-presigner`, and `pdfjs-dist` in `backend/package.json`, and install `supertest` as a backend dev dependency exactly as researched. Extend `backend/.env.example`, `backend/src/config/env.ts`, and `backend/src/test/setup.ts` with the exact storage keys `STORAGE_BUCKET`, `STORAGE_REGION`, `STORAGE_ENDPOINT`, `STORAGE_ACCESS_KEY_ID`, `STORAGE_SECRET_ACCESS_KEY`, and `STORAGE_FORCE_PATH_STYLE`. Create Wave 0 test files at `backend/src/modules/documents/documents.service.test.ts` and `backend/src/modules/documents/documents.routes.test.ts` with concrete case names `initializes a PDF upload`, `lists only owner documents`, `returns a read URL only for ready docs`, `deletes storage and metadata`, `persists metadata and status`, and `marks finalize failures as failed`.</action>
  <verify>
    <automated>test -f backend/src/modules/documents/documents.service.test.ts && test -f backend/src/modules/documents/documents.routes.test.ts && rg -n 'STORAGE_BUCKET|STORAGE_REGION|STORAGE_ENDPOINT|STORAGE_ACCESS_KEY_ID|STORAGE_SECRET_ACCESS_KEY|STORAGE_FORCE_PATH_STYLE' backend/.env.example backend/src/config/env.ts backend/src/test/setup.ts && rg -n '@aws-sdk/client-s3|@aws-sdk/s3-request-presigner|pdfjs-dist|supertest' backend/package.json</automated>
  </verify>
  <acceptance_criteria>
    - `backend/.env.example` contains `STORAGE_BUCKET=`, `STORAGE_REGION=`, `STORAGE_ENDPOINT=`, `STORAGE_ACCESS_KEY_ID=`, `STORAGE_SECRET_ACCESS_KEY=`, and `STORAGE_FORCE_PATH_STYLE=`
    - `backend/src/test/setup.ts` contains defaults for the same six storage variables
    - `backend/src/modules/documents/documents.routes.test.ts` contains `initializes a PDF upload` and `deletes storage and metadata`
    - `backend/src/modules/documents/documents.service.test.ts` contains `persists metadata and status` and `marks finalize failures as failed`
    - `backend/package.json` contains `@aws-sdk/client-s3`, `@aws-sdk/s3-request-presigner`, `pdfjs-dist`, and `supertest`
  </acceptance_criteria>
  <done>The backend storage contract and Wave 0 test scaffolds exist before lifecycle route work begins.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the document schema, storage adapter, and PDF metadata helper</name>
  <files>backend/src/modules/documents/documents.model.ts, backend/src/modules/documents/documents.schemas.ts, backend/src/modules/documents/documents.storage.ts, backend/src/modules/documents/documents.pdf.ts</files>
  <read_first>backend/src/modules/auth/auth.model.ts, backend/src/modules/documents/documents.model.ts, backend/src/modules/documents/documents.schemas.ts, backend/src/modules/documents/documents.storage.ts, backend/src/modules/documents/documents.pdf.ts, .planning/REQUIREMENTS.md, .planning/phases/02-documents-and-reader/02-CONTEXT.md, .planning/phases/02-documents-and-reader/02-RESEARCH.md</read_first>
  <action>Create `backend/src/modules/documents/documents.model.ts` with a Mongoose schema keyed by `ownerId`, `title`, `originalFilename`, `mimeType`, `sizeBytes`, `pageCount`, `status`, `processing.stage`, `processing.errorCode`, `processing.errorMessage`, `storage.provider`, `storage.bucket`, `storage.key`, `storage.etag`, `createdAt`, and `updatedAt`. Use the exact user-facing status enum `uploading | processing | ready | failed` and a separate internal processing stage enum that includes `uploaded`, `metadata_extraction`, `text_extraction`, `chunking`, `embedding`, `complete`, and `failed`. This dual-state model is mandatory for locked decision `D-08`: the Phase 2 processing-state model must extend cleanly into later AI ingestion stages without forcing a Phase 3 redesign of the document record or UI badges. Create `documents.schemas.ts` with Zod contracts for `POST /api/documents` init payloads and `POST /api/documents/:documentId/finalize`. Create `documents.storage.ts` with the exact helpers `createDocumentUploadUrl`, `headDocumentObject`, `createDocumentAccessUrl`, and `deleteDocumentObject`, using `S3Client`, `PutObjectCommand`, `GetObjectCommand`, `DeleteObjectCommand`, `HeadObjectCommand`, and `getSignedUrl`; do not proxy PDF bytes through Express. Create `documents.pdf.ts` that reads uploaded object bytes with `pdfjs-dist` and returns `{ pageCount, derivedTitle }`, falling back to the original filename when the embedded title is blank.</action>
  <verify>
    <automated>rg -n 'uploading|processing|ready|failed|metadata_extraction|text_extraction|chunking|embedding|complete' backend/src/modules/documents/documents.model.ts && rg -n 'createDocumentUploadUrl|headDocumentObject|createDocumentAccessUrl|deleteDocumentObject|PutObjectCommand|GetObjectCommand|DeleteObjectCommand|HeadObjectCommand' backend/src/modules/documents/documents.storage.ts && npm --prefix backend run build</automated>
  </verify>
  <acceptance_criteria>
    - `backend/src/modules/documents/documents.model.ts` contains `status` with `uploading`, `processing`, `ready`, and `failed`
    - `backend/src/modules/documents/documents.model.ts` contains `processing.stage`
    - `backend/src/modules/documents/documents.storage.ts` contains the four helper names `createDocumentUploadUrl`, `headDocumentObject`, `createDocumentAccessUrl`, and `deleteDocumentObject`
    - `backend/src/modules/documents/documents.pdf.ts` contains `pageCount`
    - `npm --prefix backend run build` exits `0`
  </acceptance_criteria>
  <done>The backend has the durable storage and D-08-compliant dual-state schema primitives the lifecycle routes will consume in the next plan.</done>
</task>

</tasks>

<verification>
- [ ] `npm --prefix backend run build`
- [ ] `rg -n 'STORAGE_BUCKET|STORAGE_REGION|STORAGE_ENDPOINT|STORAGE_ACCESS_KEY_ID|STORAGE_SECRET_ACCESS_KEY|STORAGE_FORCE_PATH_STYLE' backend/.env.example backend/src/config/env.ts backend/src/test/setup.ts`
- [ ] `rg -n 'createDocumentUploadUrl|headDocumentObject|createDocumentAccessUrl|deleteDocumentObject|uploading|processing|ready|failed' backend/src/modules/documents`
</verification>

<success_criteria>
The repo contains a production-shaped backend foundation for document storage and lifecycle tracking, with explicit storage env configuration and reusable schema/storage helpers ready for route implementation.
</success_criteria>

<output>
After completion, create `.planning/phases/02-documents-and-reader/02-01-SUMMARY.md`
</output>
