---
phase: 02-documents-and-reader
plan: 03
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - frontend/proxy.ts
  - frontend/components/app-shell/app-sidebar.tsx
  - frontend/lib/documents.ts
  - frontend/app/(app)/library/page.tsx
  - frontend/components/library/library-screen.tsx
  - frontend/components/library/library-upload-panel.tsx
  - frontend/components/library/document-card.tsx
  - frontend/components/library/document-status-badge.tsx
  - frontend/components/library/document-delete-dialog.tsx
  - frontend/components/ui/dialog.tsx
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03
  - DOC-05
must_haves:
  truths:
    - Signed-in users can reach a dedicated `Library` route from the app sidebar per D-09 and D-10
    - The library shows document title, file size, upload date, and processing status on responsive cards per D-11 and D-12
    - Uploading a single PDF creates an immediate visible `uploading` card and transitions through the backend lifecycle per D-05 and D-06
    - Delete actions require confirmation and remove the document from the current library view without a full-page reload per D-04
  artifacts:
    - frontend/lib/documents.ts
    - frontend/app/(app)/library/page.tsx
    - frontend/components/library/library-screen.tsx
    - frontend/components/library/library-upload-panel.tsx
    - frontend/components/library/document-card.tsx
    - frontend/components/library/document-status-badge.tsx
    - frontend/components/library/document-delete-dialog.tsx
    - frontend/components/app-shell/app-sidebar.tsx
  key_links:
    - `frontend/lib/documents.ts` calls the exact backend routes created in Plan 02-02
    - `frontend/proxy.ts` protects `/library/:path*` with the same `auth_token` cookie rule as `/dashboard`
    - `frontend/components/library/library-screen.tsx` keeps non-ready documents visible but disables open navigation until status becomes `ready`
---

<objective>
Build the authenticated document library surface: navigation, typed frontend API helpers, upload UX, responsive document cards, and delete confirmation.

Purpose: This plan turns the backend document contract into the user-visible library flow required by DOC-01 through DOC-03 and DOC-05, while honoring D-09 through D-12 and the approved Phase 2 UI contract.
Output: `/library` route, `Library` nav item, upload panel, document cards/status badges, delete dialog, and typed client helpers that keep `processing` visibly observable after upload.
</objective>

<execution_context>
@/Users/satish/Desktop/AI-Powered Learning Assistant/.codex/get-shit-done/workflows/execute-plan.md
@/Users/satish/Desktop/AI-Powered Learning Assistant/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-documents-and-reader/02-CONTEXT.md
@.planning/phases/02-documents-and-reader/02-RESEARCH.md
@.planning/phases/02-documents-and-reader/02-UI-SPEC.md
@AGENTS.md
@frontend/proxy.ts
@frontend/components/app-shell/app-sidebar.tsx
@frontend/app/(app)/layout.tsx
@frontend/app/(app)/dashboard/page.tsx
@frontend/lib/api.ts
@frontend/lib/auth.ts
@backend/src/modules/documents/documents.routes.ts

<interfaces>
From `frontend/lib/api.ts`:
```ts
export async function apiFetch<T>(path: string, init?: RequestInit): Promise<T>;
export async function serverApiFetch<T>(
  path: string,
  init?: RequestInit,
  cookieHeader?: string,
  baseUrl?: string,
): Promise<T>;
```

From `frontend/lib/auth.ts`:
```ts
export async function getCurrentUserServer();
```

From `frontend/proxy.ts`:
```ts
const AUTH_COOKIE_NAME = "auth_token";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the authenticated shell for the Library route and typed document API helpers</name>
  <files>frontend/proxy.ts, frontend/components/app-shell/app-sidebar.tsx, frontend/lib/documents.ts</files>
  <read_first>frontend/proxy.ts, frontend/components/app-shell/app-sidebar.tsx, frontend/lib/api.ts, frontend/lib/auth.ts, frontend/lib/documents.ts, backend/src/modules/documents/documents.routes.ts, .planning/phases/02-documents-and-reader/02-CONTEXT.md, .planning/phases/02-documents-and-reader/02-UI-SPEC.md</read_first>
  <action>Update `frontend/proxy.ts` so the protected matcher covers `"/library/:path*"` in addition to `"/dashboard/:path*"`, still using the `auth_token` cookie name. Convert `frontend/components/app-shell/app-sidebar.tsx` into a route-aware sidebar that exposes exactly two live primary items, `Dashboard` and `Library`, per D-10; use `href="/dashboard"` and `href="/library"` and keep the Phase 1 informational footer copy if it still fits. Create `frontend/lib/documents.ts` with exact frontend types `DocumentStatus = "uploading" | "processing" | "ready" | "failed"` and `StudyDocument`, plus these exact helpers: `getDocumentsServer`, `getDocumentById`, `initializeDocumentUpload`, `finalizeDocumentUpload`, `deleteDocument`, and `uploadDocumentFile`. `uploadDocumentFile(file)` must call `POST /api/documents`, issue a browser `PUT` directly to the returned `uploadUrl` with the returned `uploadHeaders`, then call `POST /api/documents/:documentId/finalize` and return the immediate finalized document in its current `processing | ready | failed` state; do not wait for `ready` inside the helper and do not send the PDF bytes through `apiFetch`.</action>
  <verify>
    <automated>rg -n '"/library/:path\\*"|pathname.startsWith\\("/library"' frontend/proxy.ts && rg -n 'href=\"/dashboard\"|href=\"/library\"|Dashboard|Library' frontend/components/app-shell/app-sidebar.tsx && rg -n 'DocumentStatus|StudyDocument|getDocumentsServer|getDocumentById|initializeDocumentUpload|finalizeDocumentUpload|deleteDocument|uploadDocumentFile|POST /api/documents' frontend/lib/documents.ts && bun --cwd frontend run build</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/proxy.ts` contains `"/library/:path*"`
    - `frontend/components/app-shell/app-sidebar.tsx` contains `href="/dashboard"` and `href="/library"`
    - `frontend/lib/documents.ts` contains `DocumentStatus = "uploading" | "processing" | "ready" | "failed"`
    - `frontend/lib/documents.ts` contains the exact helper names `getDocumentsServer`, `getDocumentById`, `initializeDocumentUpload`, `finalizeDocumentUpload`, `deleteDocument`, and `uploadDocumentFile`
    - `bun --cwd frontend run build` exits `0`
  </acceptance_criteria>
  <done>The app shell recognizes the library as a first-class protected route and the frontend has a typed client for all document lifecycle operations.</done>
</task>

<task type="auto">
  <name>Task 2: Build the `/library` route with upload panel, responsive card grid, and status-aware open actions</name>
  <files>frontend/app/(app)/library/page.tsx, frontend/components/library/library-screen.tsx, frontend/components/library/library-upload-panel.tsx, frontend/components/library/document-card.tsx, frontend/components/library/document-status-badge.tsx, frontend/lib/documents.ts</files>
  <read_first>frontend/app/(app)/layout.tsx, frontend/app/(app)/dashboard/page.tsx, frontend/app/(app)/library/page.tsx, frontend/components/library/library-screen.tsx, frontend/components/library/library-upload-panel.tsx, frontend/components/library/document-card.tsx, frontend/components/library/document-status-badge.tsx, frontend/lib/documents.ts, .planning/phases/02-documents-and-reader/02-CONTEXT.md, .planning/phases/02-documents-and-reader/02-UI-SPEC.md</read_first>
  <action>Create `frontend/app/(app)/library/page.tsx` as a server-authenticated route that calls `getDocumentsServer()` and passes the result into a client `LibraryScreen`. Build `LibraryScreen` so it renders the Phase 2 top upload section and a responsive card grid: one column on mobile, two on tablet, three on desktop per D-11. Build `library-upload-panel.tsx` as the single-file upload surface using the exact CTA copy `Upload PDF`, inline validation copy `Only PDF files are supported.`, and empty-state copy from the UI spec: heading `Build your document library` and body `Upload a PDF to start reading inside the app and prepare it for AI study tools.`. When a file is selected, insert an optimistic card at the top of local state with `status: "uploading"` before the `PUT` starts; after `uploadDocumentFile` resolves, replace that optimistic item with the backend document returned from finalize, even when its status is still `processing`. In `LibraryScreen`, add explicit polling or timed revalidation that calls `getDocumentById(document.id)` every `1200ms` for documents in `processing` until they settle to `ready` or `failed`, updating the existing card in place so the user visibly sees `uploading -> processing -> ready|failed` without the card disappearing. Build `document-card.tsx` and `document-status-badge.tsx` so each card shows `title`, formatted `sizeBytes`, formatted `createdAt`, a text+icon status badge, and two actions: `Open` and `Delete`. Keep `Open` disabled unless `status === "ready"` and route ready documents to `/library/${document.id}` per D-13.</action>
  <verify>
    <automated>rg -n 'Build your document library|Upload a PDF to start reading inside the app and prepare it for AI study tools.|Upload PDF|Only PDF files are supported.' frontend/app/'(app)'/library/page.tsx frontend/components/library/library-screen.tsx frontend/components/library/library-upload-panel.tsx && rg -n 'uploading|processing|ready|failed|getDocumentById|1200|setInterval|setTimeout|/library/\\$\\{document.id\\}|disabled' frontend/components/library/document-card.tsx frontend/components/library/document-status-badge.tsx frontend/components/library/library-screen.tsx frontend/lib/documents.ts && bun --cwd frontend run build</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/components/library/library-upload-panel.tsx` contains `Upload PDF`
    - `frontend/components/library/library-upload-panel.tsx` contains `Only PDF files are supported.`
    - `frontend/components/library/library-screen.tsx` contains `Build your document library`
    - `frontend/components/library/document-status-badge.tsx` contains all four labels `uploading`, `processing`, `ready`, and `failed`
    - `frontend/components/library/library-screen.tsx` contains `getDocumentById` and polling logic for `processing`
    - `frontend/components/library/document-card.tsx` contains `Open` and `Delete`
    - `frontend/components/library/document-card.tsx` contains `/library/${document.id}` or equivalent route interpolation
  </acceptance_criteria>
  <done>The library route shows the user's documents as status-aware study cards and supports the single-file upload flow required by D-05 through D-07.</done>
</task>

<task type="auto">
  <name>Task 3: Add destructive confirmation and in-place removal for owned documents</name>
  <files>frontend/components/ui/dialog.tsx, frontend/components/library/document-delete-dialog.tsx, frontend/components/library/document-card.tsx, frontend/components/library/library-screen.tsx</files>
  <read_first>frontend/components/ui/button.tsx, frontend/components/ui/dialog.tsx, frontend/components/library/document-delete-dialog.tsx, frontend/components/library/document-card.tsx, frontend/components/library/library-screen.tsx, frontend/lib/documents.ts, .planning/phases/02-documents-and-reader/02-CONTEXT.md, .planning/phases/02-documents-and-reader/02-UI-SPEC.md</read_first>
  <action>Create `frontend/components/ui/dialog.tsx` using the shadcn dialog pattern if it does not already exist, then build `frontend/components/library/document-delete-dialog.tsx` around it. Use the exact destructive copy from the UI contract: title `Delete document` and body `Delete "{title}"? This removes the PDF from your library and storage.`. Wire both the card-level delete action and the dialog confirm action through `deleteDocument(document.id)`, then remove the deleted record from the current `LibraryScreen` state array without a full page reload. Keep destructive buttons styled with the existing `destructive` variant and do not add toast-only confirmation; the dialog is the mandatory confirmation surface per D-04.</action>
  <verify>
    <automated>rg -n 'Delete document|This removes the PDF from your library and storage\\.' frontend/components/library/document-delete-dialog.tsx && rg -n 'deleteDocument\\(|setDocuments|filter\\(' frontend/components/library/document-delete-dialog.tsx frontend/components/library/library-screen.tsx frontend/components/library/document-card.tsx && bun --cwd frontend run build</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/components/library/document-delete-dialog.tsx` contains `Delete document`
    - `frontend/components/library/document-delete-dialog.tsx` contains `This removes the PDF from your library and storage.`
    - `frontend/components/library/library-screen.tsx` contains `filter(` or equivalent state removal logic after delete
    - `frontend/components/library/document-card.tsx` triggers the delete dialog rather than deleting inline
    - `bun --cwd frontend run build` exits `0`
  </acceptance_criteria>
  <done>Library deletion is explicit, consistent with D-04, and updates the UI immediately after success.</done>
</task>

</tasks>

<verification>
- [ ] `bun --cwd frontend run build`
- [ ] `rg -n 'href="/library"|Build your document library|Upload PDF|Delete document' frontend`
- [ ] Ready documents navigate to `/library/[documentId]`; non-ready documents keep `Open` disabled in the rendered UI
</verification>

<success_criteria>
Signed-in users can upload a single PDF into a dedicated library route, see lifecycle-aware metadata cards immediately, and remove owned documents with a confirmation dialog and no full-page reload.
</success_criteria>

<output>
After completion, create `.planning/phases/02-documents-and-reader/02-03-SUMMARY.md`
</output>
